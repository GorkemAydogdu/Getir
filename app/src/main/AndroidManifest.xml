<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.h5190063_gorkemaydogdu_getir">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".GetirApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.H5190063_GorkemAydogduGetir">
        <activity android:name=".activity.UrunDetayActivity"></activity>
        <activity android:name=".activity.UrunlerActivity" />
        <activity android:name=".activity.KategoriActivity" />
        <activity android:name=".activity.GirisActivity" />
        <activity android:name=".activity.KayitOlActivity" />
        <activity android:name=".activity.SplashEkraniActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>